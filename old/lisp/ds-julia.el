(use-package julia-mode
             :ensure t
             :config
             )


(use-package julia-shell-mode
             :ensure t
             :config
             )


(use-package ess
             :ensure t)

(provide 'ds-julia)
